SET( PATH serial )
STRING( REPLACE "/" "_" PREFIX "${PATH}" )

FILE( GLOB ${PREFIX}_SRCS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} ${PATH}/*.cpp )
FILE( GLOB ${PREFIX}_HEADER RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} ${PATH}/*.h )
FILE( GLOB ${PREFIX}_PRIVATE_HEADER RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} ${PATH}/*.hpp )

MY_WRAP_CPP(
	${PREFIX}_MOC_SRCS
	"${PREFIX}"
	${${PREFIX}_HEADER}
	${${PREFIX}_PRIVATE_HEADER}
)

SOURCE_GROUP( ${PREFIX} FILES ${${PREFIX}_SRCS} ${${PREFIX}_HEADER} ${${PREFIX}_PRIVATE_HEADER} )

IF( DO_COMMON_INSTALL )
INSTALL(
	FILES
	${${PREFIX}_HEADER}
	DESTINATION ${REC_ROBOTINOXT_INCLUDE_INSTALL_DIR}/${PATH}
)
ENDIF( DO_COMMON_INSTALL )

# For API1!
IF( INSTALL_REC_ROBOTINOXT )
	INSTALL(
		FILES
		${${PREFIX}_HEADER}
		DESTINATION ${INCLUDE_INSTALL_DIR}/rec/robotinoxt/${PATH}
	)
ENDIF( INSTALL_REC_ROBOTINOXT )

SET(
	SRCS
	${SRCS}
	${${PREFIX}_SRCS}
)

SET(
	HEADER
	${HEADER}
	${${PREFIX}_HEADER}
)

SET(
	PRIVATE_HEADER
	${PRIVATE_HEADER}
	${${PREFIX}_PRIVATE_HEADER}
)

SET(
	MOC_SRCS
	${MOC_SRCS}
	${${PREFIX}_MOC_SRCS}
)
